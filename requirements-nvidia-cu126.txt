# Windows x64 NVIDIA portable profile for CUDA 12.6.
# Torch is installed separately from PyTorch's official cu126 index by the
# build script so a machine-wide CPU mirror cannot silently replace it.
-r requirements-runtime-common.txt
paddlepaddle-gpu @ https://paddle-whl.bj.bcebos.com/stable/cu126/paddlepaddle-gpu/paddlepaddle_gpu-3.3.1-cp312-cp312-win_amd64.whl
nvidia-cuda-runtime-cu12==12.6.77
# Paddle's cu126 wheel metadata requests 9.5.1.17, but the loaded binary reports
# it was compiled against cuDNN 9.9. Use the binary-compatible version; OCR and
# Torch are isolated in separate processes to prevent DLL mixing.
nvidia-cudnn-cu12==9.9.0.52
nvidia-cublas-cu12==12.6.4.1
nvidia-cufft-cu12==11.3.0.4
nvidia-curand-cu12==10.3.7.77
nvidia-cusolver-cu12==11.7.1.2
nvidia-cusparse-cu12==12.5.4.2
nvidia-nvjitlink-cu12==12.6.85
